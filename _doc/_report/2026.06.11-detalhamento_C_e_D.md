# Detalhamento Grupos C e D

**Data:** 2026-06-11  
**Contexto:** Domínio corrigido e commitado em `607ad15`. Esta fase cobre UI, testes e operação.

---

## Grupo C — Trabalho Real Pendente

### C1 — `src/lora_antenna/app.py`

**Problema:** arquivo não existe. Sem ele nenhuma página Streamlit é alcançável.

**Implementar:**

```python
import streamlit as st

st.set_page_config(
    page_title="Simulador LoRa",
    layout="wide",
)

pages = {
    "Simulação Standalone": "pages/standalone",
    "Campus KML": "pages/campus",
}
```

- Roteamento multi-página via `st.navigation` (Streamlit ≥ 1.36) ou sidebar manual
- Session state defaults inicializados uma vez (não em cada rerun)
- Zero cálculo RF; zero import de `gis/` ou `propagation/` diretamente

**Adicionar em `pyproject.toml`:**

```toml
"streamlit>=1.36.0",
"folium>=0.17.0",
"streamlit-folium>=0.22.0",
```

**Remover:** nada.

---

### C2 — `src/lora_antenna/pages/standalone.py`

**Problema:** `pages/` não existe. UI standalone simula enlace ponto-a-ponto sem KML — usuário insere escalares, recebe resultado RF.

**Implementar — entradas (`st.sidebar`):**

| Parâmetro | Widget | Default | Restrição |
|-----------|--------|---------|-----------|
| Frequência (MHz) | `st.selectbox` | 915.2 | Apenas os 8 canais ANATEL |
| Potência TX (dBm) | `st.slider` | 14.0 | −20 a 30 |
| Ganho antena (dBi) | `st.number_input` | 2.15 | ≥ 0 |
| Perdas extras (dB) | `st.number_input` | 0.0 | ≥ 0 |
| Distância 2D (m) | `st.number_input` | 300.0 | > 0 |
| Distância 3D (m) | `st.number_input` | 300.0 | ≥ dist 2D |
| Sensibilidade RX (dBm) | `st.number_input` | −137.0 | < 0 |
| SF | `st.selectbox` | 12 | 7–12 |

**Implementar — fluxo de cálculo:**

```python
from lora_antenna.propagation.batch import (
    execute_link_batch, LinkBatchRequest, ScalarLinkInput,
)
from lora_antenna.antenna.base import Antenna

scalar = ScalarLinkInput(
    origin_label="TX",
    dest_label="RX",
    distance_m=dist_2d,
    distance_3d_m=dist_3d,
    total_extra_loss_db=extra_loss,
    n_buildings_crossed=0,
)
request = LinkBatchRequest(
    pairs=[scalar],
    antenna=Antenna(gain_dbi=gain, losses_db=0.0),
    tx_power_dbm=tx_power,
    frequency_hz=freq_hz,
    rx_sensitivity_dbm=rx_sens,
    sf=sf,
)
results = execute_link_batch(request)
```

**Implementar — saída:**

- `st.metric` para `received_power_dbm` e `link_margin_db`
- Indicador visual: verde (`LOW + feasible=True`), amarelo (`MEDIUM`), vermelho (`HIGH / feasible=False`)
- Tabela: todos os campos de `LinkBatchResult`
- Filtro: todos / só viáveis (`feasible=True`) / só inviáveis (`feasible=False`)

**Não implementar:**
- Nenhum import de `gis/`
- Nenhuma chamada a `run_link_budget_batch()`
- Nenhum cálculo Friis direto — apenas `execute_link_batch()`

**Remover:** nada (arquivo novo).

---

### C3 — `src/lora_antenna/pages/campus.py`

**Problema:** não existe. Orquestra o fluxo completo KML → RF → SIR → mapa → canais.

**Implementar — 5 fases sequenciais:**

#### Fase 1 — Upload e validação KML

```python
from lora_antenna.gis.kml_parser import parse_kml

uploaded = st.file_uploader("KML do campus", type=["kml"])
if uploaded:
    doc = parse_kml(uploaded)
    # reset downstream ao trocar KML
    st.session_state.update({
        "kml_document": doc,
        "distance_matrix": None,
        "link_results": None,
        "sir_matrix": None,
        "channel_assignments": None,
    })
```

- Exibir contagem de pontos e polígonos carregados
- Erro legível se KML inválido (sem pontos, namespace errado)

#### Fase 2 — Configuração RF

- Mesmos parâmetros de C2 em `st.sidebar`
- Ao mudar qualquer parâmetro RF: `st.session_state["link_results"] = None`

#### Fase 3 — Simulação multiponto

```python
from lora_antenna.gis.multi_link import run_link_budget_batch, results_to_sir_matrix

results = run_link_budget_batch(
    doc, antenna,
    frequency_hz=freq_hz,
    tx_power_dbm=tx_power,
    rx_sensitivity_dbm=rx_sens,
    sf=sf,
    extra_losses_db=extra_loss,
)
st.session_state["link_results"] = results
st.session_state["sir_matrix"] = results_to_sir_matrix(results)
```

- Tabela com filtro `feasible`
- Coluna `sir_decodable` com ícone ✓/✗

#### Fase 4 — Mapa (folium)

| Camada | Dados | Estilo |
|--------|-------|--------|
| Nós | `doc.points` | marcador azul com label |
| Prédios | `polygons_by_role(BUILDING)` | polígono cinza |
| Obstáculos | `polygons_by_role(OBSTACLE)` | polígono laranja |
| Enlace `LOW + feasible` | `link_risk=LOW, feasible=True` | linha verde |
| Enlace `MEDIUM + feasible` | `link_risk=MEDIUM, feasible=True` | linha amarela |
| Enlace inviável | `feasible=False` | linha vermelha |
| Enlace `sir_decodable=False` | sobre qualquer cor | linha pontilhada |

#### Fase 5 — Otimização de canais

```python
from lora_antenna.network.channel_plan import (
    build_interference_graph,
    greedy_channel_assignment,
    count_channel_collisions,
)

graph = build_interference_graph(st.session_state["sir_matrix"], sf=sf)
node_labels = [p.label for p in doc.points]
assignments = greedy_channel_assignment(node_labels, graph)
collisions = count_channel_collisions(assignments, graph)
st.session_state["channel_assignments"] = assignments

st.metric("Colisões de canal", collisions)
```

- Tabela: nó → canal MHz
- Mapa com marcadores coloridos por canal

**Session state defaults (inicializar em `app.py`):**

```python
CAMPUS_DEFAULTS = {
    "kml_document": None,
    "distance_matrix": None,
    "link_results": None,
    "sir_matrix": None,
    "channel_assignments": None,
}
```

**Não implementar:**
- Nenhum cálculo SIR direto — usar `results_to_sir_matrix()`
- Nenhuma coloração de grafo direta — usar `greedy_channel_assignment()`
- Não manter resultados stale após troca de KML ou parâmetros RF

**Remover:** nada (arquivo novo).

---

### C4 — `Dockerfile` + `docker-compose.yml`

**Problema:** não existem. Pré-requisito: C1 (`app.py`) deve existir.

**Implementar — `Dockerfile`:**

```dockerfile
FROM python:3.13-slim

WORKDIR /app

RUN pip install uv

COPY pyproject.toml uv.lock ./
RUN uv sync --no-dev

COPY src/ ./src/

EXPOSE 3953

CMD ["uv", "run", "streamlit", "run", "src/lora_antenna/app.py", \
     "--server.port", "3953", "--server.address", "0.0.0.0"]
```

**Implementar — `docker-compose.yml`:**

```yaml
services:
  lora-ui:
    build: .
    ports:
      - "3953:3953"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
```

**Corrigir antes de criar:**
- `uv.lock` deve estar atualizado após adicionar `streamlit`/`folium` em `pyproject.toml`
- Confirmar que `uv sync --no-dev` resolve todas as dependências de runtime

**Não fazer:**
- Não usar `pip install -r requirements.txt` — projeto usa `uv`
- Não copiar `.venv/` para a imagem

**Remover:** nada (arquivos novos).

---

## Grupo D — Validações a Executar

### D1 — Boundary check final

**Status atual:** passou na sessão 2026-06-11.  
**Problema:** não está automatizado. Qualquer alteração futura em `propagation/`, `core/` ou `antenna/` pode introduzir violação sem detecção.

**Executar agora:**

```powershell
rg -n "from lora_antenna\.(gis|network)|import lora_antenna\.(gis|network)" `
   src/lora_antenna/propagation `
   src/lora_antenna/core `
   src/lora_antenna/antenna
```

**Resultado esperado:** zero ocorrências (exit 1 do rg = sem matches = PASS).

**Implementar (opcional):** script `scripts/check_boundary.ps1` ou hook `pre-commit`:

```yaml
# .pre-commit-config.yaml
- repo: local
  hooks:
    - id: boundary-check
      name: Bloco 1 boundary
      entry: rg --quiet "from lora_antenna\.(gis|network)" src/lora_antenna/propagation src/lora_antenna/core src/lora_antenna/antenna
      language: system
      pass_filenames: false
```

**Corrigir:** nada — check já passou.  
**Remover:** nada.

---

### D2 — Suite de testes (pytest)

**Status atual:** `tests/` não existe; `pytest` não está em `pyproject.toml`.

**Problema:** sem testes automatizados, toda validação é manual e não rastreável por CI.

**Corrigir em `pyproject.toml`:**

```toml
[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
]
```

**Instalar:**

```bash
uv sync --extra dev
```

**Implementar — estrutura mínima:**

```
tests/
├── __init__.py
├── test_constants.py
├── test_friis.py
├── test_contracts.py
├── test_executor.py
├── test_distance_matrix.py
└── test_reference_tables.py
```

**Teste crítico — executor sem KML:**

```python
def test_execute_link_batch_feasible():
    from lora_antenna.propagation.batch import (
        execute_link_batch, LinkBatchRequest, ScalarLinkInput,
    )
    from lora_antenna.antenna.base import Antenna

    inp = ScalarLinkInput(
        origin_label="P1", dest_label="P2",
        distance_m=399.0, distance_3d_m=399.0,
        total_extra_loss_db=0.0,
    )
    req = LinkBatchRequest(
        pairs=[inp],
        antenna=Antenna(gain_dbi=2.15),
        frequency_hz=915_200_000.0,
    )
    results = execute_link_batch(req)
    r = results[0]
    assert r.feasible == (r.link_margin_db > 0.0)
    assert r.sir_db == float("inf")
    assert r.sir_decodable is True
```

**Teste crítico — boundary (sem KML em propagation):**

```python
def test_no_gis_import_in_propagation():
    import ast, pathlib
    root = pathlib.Path("src/lora_antenna/propagation")
    for f in root.rglob("*.py"):
        tree = ast.parse(f.read_text())
        for node in ast.walk(tree):
            if isinstance(node, (ast.Import, ast.ImportFrom)):
                names = getattr(node, "names", [])
                module = getattr(node, "module", "") or ""
                for alias in names:
                    full = f"{module}.{alias.name}" if module else alias.name
                    assert "lora_antenna.gis" not in full, f"GIS import in {f}: {full}"
```

**Remover:** nada.

---

### D3 — Verificar `delta_reference_m` P1–P2

**Status:** report table registra Haversine = `399.0` m; MATLAB flat-earth = `399.4` m.

**Executar:**

```python
from lora_antenna.gis.reference_tables import delta_reference_m
print(delta_reference_m("P1", "P2", 399.0))   # esperado: -0.4
```

**Interpretação:**
- Delta `−0.4 m` em distância de ~400 m = diferença de método, não erro
- Haversine (elipsoide WGS84) vs flat-earth (projeção plana com cos(lat_avg))
- Divergência cresce com a distância — para P4–P8 (~1306 m) pode chegar a ~1–2 m

**Corrigir:** nada no código — comportamento esperado e documentado.  
**Adicionar:** nota no `doc/manual/metricas_rf_e_interpretacao.md` explicando a divergência (se necessário para o usuário final).

---

## Dependências entre Itens

```
pyproject.toml  ──► C1 (app.py)
                │      ├──► C2 (standalone.py)
                │      └──► C3 (campus.py)  ──► C4 (Dockerfile)
                └──► D2 (pytest)

D1  ─ independente, executável agora
D3  ─ independente, executável agora
```

## Ordem de Execução Recomendada

```
D1 → D3 → pyproject.toml (dev deps + streamlit) → D2 → C1 → C2 → C3 → C4
```
